# Tableau Électrique Citoyen — Client

## Install the dependencies

Install PNPM if needed:

```bash
npm i -g pnpm
```

Then install the project dependencies:

```bash
pnpm i
```

## Build the application

```bash
pnpm build
```

## Run the application in development mode

```bash
pnpm start
```
